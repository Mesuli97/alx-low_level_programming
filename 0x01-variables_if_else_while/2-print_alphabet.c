#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always return 0
 */
int main(void)
{
	char mesuli;

	for (mesuli = 'a'; mesuli <= 'z'; mesuli++)
	{
		putchar(mesuli);
	}
	putchar('\n');
	return (0);
}
